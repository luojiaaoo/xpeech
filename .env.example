# Docker Compose 项目名，用作容器、网络等资源的名称前缀
COMPOSE_PROJECT_NAME=xpeech

# 后端 API 对外端口（默认：7878）
# BACKEND_PORT=7878

# Web 客户端对外端口（默认：7939）
# WEB_CLIENT_PORT=7939

# Browserless Chromium 的容器内 CDP 地址
CDP_URL=ws://browserless:3000
